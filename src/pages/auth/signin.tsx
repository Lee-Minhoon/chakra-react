import { SigninForm } from "@/containers";
import { CenteredLayout } from "@/layouts";

const SigninPage = () => {
  return (
    <CenteredLayout>
      <SigninForm />
    </CenteredLayout>
  );
};

export default SigninPage;
